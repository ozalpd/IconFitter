<Window x:Class="IconFitter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IconFitter"
        xmlns:vm="clr-namespace:IconLib.ViewModels;assembly=IconLib"
        mc:Ignorable="d"
        Title="Icon Fitter" Icon="ArtAssets\App-Icon.ico"
        Height="600" Width="800" MinHeight="320" MinWidth="640"
        Loaded="Window_Loaded" Closing="Window_Closing" SizeChanged="Window_SizeChanged">
    <Window.DataContext>
        <vm:IconFitterVM />
    </Window.DataContext>
    <Window.Resources>
        <Style x:Key="toolbarButtons" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="28" />
            <Setter Property="Width" Value="32" />
            <Setter Property="Margin" Value="4,0,3,0" />
            <Setter Property="Padding" Value="2"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="inHorizStack">
            <Setter Property="Margin" Value="4,8" />
        </Style>
        <Style TargetType="TextBox" x:Key="inHorizTextBox">
            <Setter Property="Margin" Value="4,8,4,4" />
        </Style>
        <BitmapImage x:Key="uriNew" UriSource="ArtAssets/NewDocument-16.png"/>
        <BitmapImage x:Key="uriOpen" UriSource="ArtAssets/open-16.png"/>
        <BitmapImage x:Key="uriSave" UriSource="ArtAssets\save-16.png"/>
        <Image x:Key="imgNew" Source="{StaticResource uriNew}" Stretch="Uniform"/>
        <Image x:Key="imgOpen" Source="{StaticResource uriOpen}" Stretch="None"/>
        <Image x:Key="imgSave" Source="{StaticResource uriSave}" Stretch="Uniform"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="96"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="96"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="96"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="32"/>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
            <Menu Height="28" VerticalAlignment="Top" Width="72">
                <MenuItem Header="_File" Height="28">
                    <MenuItem Header="_New Project" Command="{Binding NewProjectCommand}" InputGestureText="Ctrl+N">
                        <MenuItem.Icon>
                            <Image Source="{StaticResource uriNew}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="_Open Project" Command="{Binding OpenProjectCommand}" InputGestureText="Ctrl+O">
                        <MenuItem.Icon>
                            <Image Source="{StaticResource uriOpen}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{Binding MostRecentProjectFile.Name,StringFormat='Open \{0\}'}"
                                      Command="{Binding OpenMostRecentProjectCommand}"
                                      InputGestureText="Ctrl+R"/>
                    <MenuItem Header="_Save Project" Command="{Binding SaveProjectCommand}" InputGestureText="Ctrl+S">
                        <MenuItem.Icon>
                            <Image Source="{StaticResource uriSave}"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="_Quit" Command="{Binding CloseCommand, Mode=OneWay}" InputGestureText="Ctrl+Q"/>
                </MenuItem>
                <MenuItem Header="_Edit" Height="28">
                    <MenuItem Header="_Cut" InputGestureText="Ctrl+X" IsEnabled="False"/>
                    <MenuItem Header="_Copy" InputGestureText="Ctrl+C" IsEnabled="False"/>
                    <MenuItem Header="_Paste" InputGestureText="Ctrl+V" IsEnabled="False"/>
                </MenuItem>
            </Menu>

            <Button Command="{Binding OpenCommand}" Content="{Binding Mode=OneWay, Source={StaticResource imgOpen}}" Style="{StaticResource toolbarButtons}"/>
            <TextBlock Text="{Binding ImageFile.Size, Mode=OneWay, StringFormat=Size: \{0\}}" Style="{StaticResource inHorizStack}"/>
        </StackPanel>


        <TextBlock Grid.Column="2" Grid.Row="0" Style="{StaticResource inHorizStack}" Text="Target Folder:"/>
        <TextBox Grid.Column="3" Grid.Row="0" Style="{StaticResource inHorizTextBox}" Text="{Binding TargetDirectory}"/>

        <ScrollViewer x:Name="ImageViewScroll" Grid.ColumnSpan="4" Grid.Row="4" Margin="2,0" Background="#686868" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Width="{Binding ImageViewSize.Width}" Height="{Binding ImageViewSize.Height}">
                <Grid.Background>
                    <DrawingBrush Viewport="0,0,30,30" ViewportUnits="Absolute" TileMode="Tile">
                        <DrawingBrush.Drawing>
                            <DrawingGroup>
                                <DrawingGroup.Children>
                                    <GeometryDrawing Brush="#b8b8b8">
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <RectangleGeometry Rect="50,25,25,25" />
                                                <RectangleGeometry Rect="25,50,25,25" />
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingGroup.Children>
                            </DrawingGroup>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Grid.Background>
                <Image Source="{Binding ImageFile.BitmapSource, Mode=OneWay}"/>
            </Grid>
        </ScrollViewer>


        <!--Resize Image Elements-->
        <Button Grid.Column="0" Grid.Row="1" Content="Resize Image" Margin="4,4" Command="{Binding ResizeCommand}"/>

        <Grid Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Style="{StaticResource inHorizStack}" Text="Target Width:" ToolTip="Target Width"/>
            <TextBox Grid.Column="1" Style="{StaticResource inHorizTextBox}" Text="{Binding TargetWidth}" TextAlignment="Right"/>

            <Button x:Name="btnSetHeightToWidth" Grid.Column="2" Margin="0,0,8,0" Content="→" ToolTip="Set Target Height to Target Width" Click="BtnSetHeightToWidth_Click" FontSize="16"/>

            <TextBlock Grid.Column="3" Style="{StaticResource inHorizStack}" Text="Height:" ToolTip="Target Height"/>
            <TextBox Grid.Column="4" Style="{StaticResource inHorizTextBox}" Text="{Binding TargetHeight}" TextAlignment="Right"/>

            <TextBlock Grid.Column="5" Style="{StaticResource inHorizStack}" Text="Quality:" ToolTip="Target Quality"/>
            <TextBox Grid.Column="6" Style="{StaticResource inHorizTextBox}" Text="{Binding TargetQuality}" TextAlignment="Right"/>

            <TextBlock Grid.Column="7" Style="{StaticResource inHorizStack}" Text="Extension:" ToolTip="Target Extension"/>
            <TextBox Grid.Column="8" Style="{StaticResource inHorizTextBox}" Text="{Binding TargetExtension}"/>
        </Grid>

        <TextBox Grid.Column="4" Grid.Row="1" Style="{StaticResource inHorizTextBox}" Text="{Binding Zoom}"/>

        <!--Resize Image Ends-->
        <Grid Grid.Column="1" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" Grid.Row="2" Style="{StaticResource inHorizStack}" Text="Keep Aspect Ratio" ToolTip="Keep Aspect Ratio"/>
            <CheckBox Grid.Column="0" Margin="4,10,0,0" IsChecked="{Binding KeepAspectRatio}" ToolTip="Keep Aspect Ratio" />

            <TextBlock Grid.Column="3" Style="{StaticResource inHorizStack}" Text="Optimize Target" ToolTip="Optimize Target after operation"/>
            <CheckBox Grid.Column="2"  Margin="4,10,0,0" IsChecked="{Binding OptimizeTarget}" ToolTip="Optimize Target after operation" />
        </Grid>

        <Button Grid.Column="0" Grid.Row="2" Content="Make Icon File" Margin="4,4" Command="{Binding MakeWin32Icon}"/>
        <Button Grid.Column="2" Grid.Row="2" Content="Optimize" Margin="4,4" Command="{Binding OptimizeTargetImage}"/>
        <TextBlock Grid.Column="3" Grid.ColumnSpan="2" Grid.Row="2" Style="{StaticResource inHorizStack}" Text="{Binding ElapsedTime, Mode=OneWay, StringFormat=ElapsedTime: \{0\}}"/>

    </Grid>
</Window>
